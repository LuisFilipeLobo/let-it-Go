# Este app busca por IPs e nomes de Servidores

# Exemplos de execução:

# Para buscar por IPs
./linha-comando ip --host google.com

# Para buscar por nomes de Servidores
./linha-comando server --host google.com
